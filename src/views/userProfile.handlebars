<div class="container mt-5">
    <h1>Perfil de Usuario</h1>
    <p class="lead">Nombre: {{user.user.first_name}}</p>
    <p class="lead">Apellido: {{user.user.last_name}}</p>
    <p class="lead">Email: {{user.user.email}}</p>
    <p class="lead">Edad: {{user.user.age}}</p>
    


    <a href="/home" class="mt-3">
        <button class="btn btn-success">Volver</button>
    </a>

    {{#if isAdmin}}
        <a href="/usersmanagement" class="btn btn-warning">Manejo de usuarios</a>
    {{else}}
        <button id="recovery" class=" btn btn-primary">Recuperar Contraseña</button>
    {{/if}}
    
    <a href="/uploadDoc" class=" btn btn-primary">Subir Documentos</a>

   


    <button id="logoutButton" class="btn btn-danger">Logout</button>

    



</div>
